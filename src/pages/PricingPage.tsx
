import { motion } from 'framer-motion'
import { Check, Sparkles, Crown, Trophy } from 'lucide-react'
import { AnimatedBackground } from '@/components/AnimatedBackground'
import { GlowButton } from '@/components/GlowButton'

const plans = [
  {
    name: 'Starter',
    price: '$500',
    period: '/month',
    icon: Sparkles,
    description: 'Perfect for solo coaches and aspiring pros',
    features: [
      'AI Coach access',
      '100 strategies/month',
      'Basic agent composition',
      'Email support',
      'Map-specific tactics'
    ],
    highlight: false
  },
  {
    name: 'Pro Team',
    price: '$2,500',
    period: '/month',
    icon: Crown,
    description: 'For competitive teams and organizations',
    features: [
      '5 AI Coach accounts',
      'Unlimited strategies',
      'VOD analysis',
      'Real-time adaptations',
      'Priority support',
      'Custom team protocols',
      'Opponent scouting'
    ],
    highlight: true
  },
  {
    name: 'Tournament',
    price: '$10,000',
    period: '/event',
    icon: Trophy,
    description: 'Elite support for tournament play',
    features: [
      'Dedicated AI coach',
      'Real-time support',
      'Live match analysis',
      'In-game coaching overlay',
      'Post-match reports',
      '24/7 hotline',
      'Custom integrations',
      'Multi-team support'
    ],
    highlight: false
  }
]

export default function PricingPage() {
  return (
    <div className="min-h-screen bg-[#0F1923] relative">
      <AnimatedBackground />
      
      <div className="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pt-24 pb-16">
        {/* Hero */}
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.6 }}
          className="text-center mb-16"
        >
          <h1 className="text-4xl md:text-6xl font-bold text-white mb-6">
            Simple, <span className="text-[#FF4655]">Transparent</span> Pricing
          </h1>
          <p className="text-xl text-[#A8B2C1] max-w-2xl mx-auto">
            Choose the plan that fits your team's needs. All plans include our core AI engine.
          </p>
        </motion.div>

        {/* Pricing Cards */}
        <div className="grid md:grid-cols-3 gap-8 mb-16">
          {plans.map((plan, index) => (
            <motion.div
              key={plan.name}
              initial={{ opacity: 0, y: 20 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.6, delay: index * 0.1 }}
              className={`relative ${
                plan.highlight 
                  ? 'md:-mt-4 md:mb-4' 
                  : ''
              }`}
            >
              {/* Highlight Badge */}
              {plan.highlight && (
                <div className="absolute -top-4 left-1/2 -translate-x-1/2 px-4 py-1 bg-[#FF4655] text-white text-sm font-semibold rounded-full">
                  Most Popular
                </div>
              )}
              
              <div className={`h-full p-8 rounded-2xl border transition-all duration-300 ${
                plan.highlight
                  ? 'bg-gradient-to-b from-[#1C2B3A] to-[#0F1923] border-[#FF4655] shadow-[0_0_40px_rgba(255,70,85,0.3)]'
                  : 'bg-[#1C2B3A]/50 border-[#2B3E50] hover:border-[#FF4655]/50'
              }`}>
                {/* Icon */}
                <div className={`w-14 h-14 rounded-xl flex items-center justify-center mb-6 ${
                  plan.highlight 
                    ? 'bg-[#FF4655]/20' 
                    : 'bg-[#0F1923] border border-[#2B3E50]'
                }`}>
                  <plan.icon className={`w-7 h-7 ${plan.highlight ? 'text-[#FF4655]' : 'text-[#A8B2C1]'}`} />
                </div>

                {/* Plan Name */}
                <h3 className="text-2xl font-bold text-white mb-2">{plan.name}</h3>
                <p className="text-[#A8B2C1] text-sm mb-6">{plan.description}</p>

                {/* Price */}
                <div className="mb-8">
                  <span className="text-5xl font-bold text-white">{plan.price}</span>
                  <span className="text-[#A8B2C1]">{plan.period}</span>
                </div>

                {/* Features */}
                <ul className="space-y-4 mb-8">
                  {plan.features.map((feature, i) => (
                    <li key={i} className="flex items-start gap-3">
                      <Check className="w-5 h-5 text-[#FF4655] flex-shrink-0 mt-0.5" />
                      <span className="text-[#A8B2C1] text-sm">{feature}</span>
                    </li>
                  ))}
                </ul>

                {/* CTA */}
                <GlowButton
                  variant={plan.highlight ? 'primary' : 'outline'}
                  className="w-full"
                >
                  Get Started
                </GlowButton>
              </div>
            </motion.div>
          ))}
        </div>

        {/* FAQ or Note */}
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.6, delay: 0.4 }}
          className="text-center"
        >
          <div className="inline-block p-6 bg-[#1C2B3A]/50 rounded-xl border border-[#2B3E50]">
            <p className="text-[#A8B2C1]">
              Need a custom solution for your organization?{' '}
              <a href="/contact" className="text-[#FF4655] hover:text-[#ff5a68] font-semibold">
                Contact us
              </a>
            </p>
          </div>
        </motion.div>
      </div>
    </div>
  )
}

